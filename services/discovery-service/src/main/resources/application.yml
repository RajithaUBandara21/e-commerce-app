spring:
  config:
      import: optional:configserver:http://localhost:8888
  application:
    name: discovery-service

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
